resources:
  pipelines:
    lakeflow_bronze:
      name: lakeflow_bronze
      libraries:
        - glob:
            include: ../../lakeflow/transformations/bronze/**
      schema: bronze
      catalog: dev
      serverless: true
      root_path: ${workspace.file_path}/lakeflow
